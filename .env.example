# Database (Railway provides this automatically)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/stratum_sourcing

# Slack integration
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_SIGNING_SECRET=your-signing-secret
SLACK_APP_TOKEN=xapp-your-app-token
SLACK_CHANNEL_SOURCING=#sourcing

# Notion integration
NOTION_API_KEY=secret_your-notion-key
NOTION_OCEAN_DATABASE_ID=your-database-id

# LLM -- Option 1 (production): OpenClaw gateway with Codex OAuth
# OpenClaw runs as sidecar, uses OAuth minter for Codex subscription access
OPENCLAW_INTERNAL_PORT=9080
OPENCLAW_GATEWAY_TOKEN=dev-token
OAUTH_MINTER_URL=https://your-minter.up.railway.app
OAUTH_MINTER_KEY=your-minter-key

# LLM -- Option 2 (fallback): Direct Anthropic API
ANTHROPIC_API_KEY=sk-ant-your-key

# LLM -- Option 3 (fallback): Direct OpenAI API key
# OPENAI_API_KEY=sk-your-key

LLM_MODEL=gpt-4o

# Cron job authentication
CRON_SECRET=your-cron-secret

# Persistent volume
DATA_DIR=/data

# Scraping settings
FETCH_TIMEOUT_SECONDS=30
FETCH_CONCURRENCY=5
BROWSER_RATE_LIMIT_SECONDS=10
